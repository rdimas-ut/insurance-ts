// Defines what group(aka as tab) can be selected

export enum SideOption {
    Home,
    Customers,
    Vendors
}

export enum CustDVOption {
    Invoices="Invoices",
    Policies="Policies",
    Census="Census",
    Back="Back"
}

export enum VendDVOption {
    Bills="Bills",
    Back="Back"
}

export enum ModalOption {
    Error="Error", 
    Census="Census",
    Invoice="Invoice",
    Bill="Bill", 
    Policy="Policy"
}